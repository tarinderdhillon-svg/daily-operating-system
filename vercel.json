{
  "buildCommand": "pnpm --filter @workspace/daily-os run build && pnpm --filter @workspace/api-server run build",
  "public": "public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
